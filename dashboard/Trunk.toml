# Trunk.toml - Build configuration for Leptos dashboard

[build]
target = "index.html"
dist = "dist"
# Disable SRI hashes - Cloudflare Auto Minify breaks integrity checks
no_sri = true

[watch]
watch = ["src", "index.html", "style.css"]

[serve]
address = "127.0.0.1"
port = 8080
